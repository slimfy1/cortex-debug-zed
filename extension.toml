id = "cortex-debug"
name = "Cortex-Debug"
description = "Debug ARM Cortex-M microcontrollers (J-Link, OpenOCD, pyOCD, ST-Link, BMP, QEMU) using the Cortex-Debug adapter."
version = "0.1.0"
schema_version = 1
authors = ["Cortex-Debug contributors", "Zed port"]
repository = "https://github.com/Marus/cortex-debug"

[debug_adapters.cortex-debug]
schema_path = "debug_adapter_schemas/cortex-debug.json"
